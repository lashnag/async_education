{
  "type": "object",
  "properties": {
    "accountPublicUid": {
      "type": "string",
      "format": "uuid"
    },
    "currentBalance": {
      "type": "integer"
    },
    "metadata": {
      "type": "object",
      "properties": {
        "eventVersion": { "type": "string" },
        "eventId": { "type": "string" },
        "eventTime": { "type": "string" },
        "producer": { "type": "string" }
      },
      "required": ["eventVersion", "eventId", "eventTime", "producer"]
    }
  },
  "required": ["accountPublicUidId", "currentBalance", "metadata"]
}
